app = "douyin-api"  # 替换为你上一步设置的应用名

[build]
  # 项目根目录有 Dockerfile，无需额外配置

[[services]]
  internal_port = 8000  # 关键：必须为8000
  protocol = "tcp"

  [[services.ports]]
    port = 80
    handlers = ["http"]

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]
